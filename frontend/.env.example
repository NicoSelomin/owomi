# ============================================================
# OWOMI Frontend — Variables d'environnement (exemple)
# Copier en `.env`. Les valeurs runtime sont gérées via
# src/environments/environment.ts (dev) et environment.prod.ts (prod).
# ============================================================

API_BASE_URL=http://localhost:8080
APP_VERSION=1.0.0
